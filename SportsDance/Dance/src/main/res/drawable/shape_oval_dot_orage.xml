<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval" >

    <!-- 填充 -->
    <!--<solid android:color="@color/colorAccent" />-->
      <solid android:color="@color/slide_view"/>
    <size
        android:width="9dp"
        android:height="9dp" />

</shape>